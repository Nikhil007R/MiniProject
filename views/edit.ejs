<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-5 text-white">

        <!-- form for input  -->
        <div class="flex space-between items-baseline" >

            <div class="createContainer w-full">
                
                <h5 class="mb-5 text-green-300">Edit your post</h5>
                <form action="/update/<%= post._id%>" method="post">
                    <textarea class="w-1/3 p-3 mb-2 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="What's in your mind?" name="content"><%= post.content %></textarea>
                    <input type="submit" class=" block px-3 py-2 bg-yellow-500 rounded-md text-black" value="Update Post" >
                </form>
                
            </div>
            <div class="logout">
                <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout</a>
            </div>
        </div>
        
    </div>
</body>
</html>